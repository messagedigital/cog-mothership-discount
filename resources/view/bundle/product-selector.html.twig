{{ form_start(form) }}
	{{ form_widget(form['bundle_id']) }}
	{% for field in form_fields %}
		{{ form_row(form[field]) }}
	{% endfor %}
	<button type="submit">{{ 'ms.discount.bundle.product_selector.button'|trans }}</button>
{{ form_end(form) }}