<div class="search">
{{ form_start(code_search_form) }}
	{{ form_row(code_search_form.term, { 'attr': {'placeholder': 'Search discounts...' } }) }}
	<button type="submit">Search</button>
{{ form_end(code_search_form) }}
</div>

<a href="{{ url('ms.cp.discount.create')}}" class="button small create discount" data-live>Add a new discount</a>
<a href="{{ url('ms.cp.discount.bundle.create')}}" class="button small create discount" data-live>Add a new bundle</a>

<ol class="menu">
	<li>
		<a href="{{ url('ms.cp.discount.listing.active') }}" data-live>Active Discounts</a>
	</li>
	<li>
		<a href="{{ url('ms.cp.discount.listing.inactive') }}" data-live>Inactive Discounts</a>
	</li>
	<li>
		<a href="{{ url('ms.cp.discount.bundle.listing') }}" data-line>Bundles</a>
	</li>
</ol>